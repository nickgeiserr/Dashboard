import React, { useEffect, useState } from 'react';
import { fetchAllMembers } from '../../Services/FirebaseService.js';

const ManageMembers = () => {
  return (
    <div className="bg-gray-900 min-h-screen">
        <h1>waiting on cloud functions</h1>
    </div>
  );
};

export default ManageMembers;
